<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.harlemknights.TimeRush">

    <application
        android:allowBackup="true"
        android:icon="@drawable/fireclock"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">

        </activity>
        <activity android:name=".PlayGameActivity" />
        <activity android:name=".InstructionActivity" />
        <activity android:name=".AfterGameActivity" />
        <activity android:name=".ReflexModeActivity" />
        <activity android:name=".ReflexLostGameActivity" />
        <activity android:name=".SplashScreenActivity">
            <intent-filter>


            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>